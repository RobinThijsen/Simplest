{
    "name": "simplest-assets",
    "version": "1.0.0",
    "main": "main.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "sass-prod": "sass --no-source-map --style=compressed scss:../dist/css",
        "js-prod": "uglifyjs main.js -o ../dist/js/main.js --compress --mangle",
        "build": "npm run sass-prod && npm run js-prod"
    },
    "author": "",
    "license": "ISC",
    "description": "",
    "devDependencies": {
        "sass": "^1.80.1"
    }
}
